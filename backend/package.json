{
    "name": "tarot-backend",
    "version": "1.0.0",
    "description": "Tarot App Backend Functions",
    "main": "index.js",
    "scripts": {
        "build": "tsc",
        "package": "npm run build && zip -r function.zip dist/ node_modules/"
    },
    "dependencies": {
        "@aws-sdk/client-cognito-identity-provider": "^3.477.0",
        "@aws-sdk/client-dynamodb": "^3.477.0",
        "@aws-sdk/util-dynamodb": "^3.477.0"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.133",
        "@types/node": "^20.10.0",
        "typescript": "^5.3.0"
    }
}